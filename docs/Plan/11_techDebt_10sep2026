## Tech Debt Checklist — by Sprint

### Sprint A - Completed on 13 Sep 2026. Commit : bb2aa7963c674f8b279089398cd85129940b4bad

---

### Sprint B — Boundary Hardening
When: Before your next release goes out (fold into the release after 1.2/V10 clear review/testing).

- [x] Fix the ColumnBoundaryDetector.kt comment/code mismatch (comment says 40pt merge gap, code uses 85pt) — **Done 13 Sep 2026** (commit `4ea625e`): kept the 85pt runtime behavior, extracted `COLUMN_MERGE_GAP_PT = 85f`, aligned comment. Verified via `:shared:testDebugUnitTest`.

*Ignored per user direction:*
- *`FLAG_SECURE` in `MainActivity.kt` (leaves screenshots enabled during testing)*
- *300-line limit "Red Zone" splits (handled opportunistically only)*

---

### Sprint C — Parsing Bug (Ignored)
- *Ignored per user direction (multi-page table continuation row drop and fixtures shelved).*

---

### Sprint D — Architecture, Test Seams & Cleanup
When: Target for upcoming cleanup cycles.

- [x] Move `FakePayslipDao.kt` / `FakePdfParser.kt` out of `commonMain` into a test-fixtures structure (removes test mocks from shipping production binary). **Done 13 Sep 2026**: confirmed the Android release APK already stripped these via R8 (`isMinifyEnabled = true`, zero non-test call sites) — the real, narrower win is iOS: the `shared` static framework exports its full public Kotlin API to Swift regardless of usage, so these `public class` fakes were leaking into `shared.framework`'s distributed interface. Fix: new KMP module `:shared-test-fixtures` (android + iosX64/iosArm64/iosSimulatorArm64 targets, no framework/linker block needed since it's never exported) holding both files at their original package path; `shared`'s and `composeApp`'s `commonTest` now depend on it (`androidUnitTest` inherits via `commonTest`). No call-site changes needed (package unchanged). Verified via `:shared:testDebugUnitTest`, `:composeApp:testDebugUnitTest`, `ktlintCheck`, `iosSimulatorArm64Test` (both modules), `:composeApp:linkDebugFrameworkIosSimulatorArm64`, and `check_tech_debt_limits.py --strict` on the moved files.
- [x] Move `calculateAssetProjections` (in `DsopAssetComparisonCard.kt`) out of the UI file into domain engine `ProjectionMath.kt` (MVVM leak). **Done 13 Sep 2026**: split rather than moved whole — `calculateAssetProjections` itself stays in `DsopAssetComparisonCard.kt` because it builds `AssetProjectionResult`, which carries `AppStrings`-derived labels that `shared` (a module `composeApp` depends on, not vice versa) cannot see. Moved only the pure math (`calculateCompoundValue`) into `shared/insights/ProjectionMath.kt` as a `public` member (had to promote from `internal` — Kotlin's `internal` is module-scoped, so `composeApp` couldn't call an `internal` member of `shared`), and `calculateAssetProjections` now calls `ProjectionMath.calculateCompoundValue(...)`. Its compound-growth test case moved to `ProjectionMathTest.kt` in `shared`; `DsopAssetComparisonCardTest.kt` keeps testing the composeApp-side wrapper unchanged. Verified via `:shared:testDebugUnitTest`, `:composeApp:testDebugUnitTest`, `ktlintCheck`, and `:composeApp:linkDebugFrameworkIosSimulatorArm64`.
- [x] Move `breakdownWellnessDrivers` scoring logic (in `InsightsWellnessLogic.kt`) out of the UI layer into domain (MVVM leak). **Done 13 Sep 2026**: moved `InsightsWellnessLogic.kt` (and its `WellnessDriversTest.kt`) from `ui/screens` into composeApp's existing `domain/` package (same module — no boundary issue, since `InsightsStrings` is also composeApp-only). Updated call sites in `MonthlySnapshot.kt` and `InsightsWellnessComponents.kt`. Verified via the same four checks above.
- [x] **Remove from Code**: Delete `ZeroPiiReportExporter.kt` and `ZeroPiiReportExporterTest.kt` completely (unwired diagnostic exporter is no longer required). **Done 13 Sep 2026**: confirmed zero call sites outside its own test via repo-wide grep, then deleted both files. Verified via `:shared:testDebugUnitTest` and `ktlintCheck`.
- [x] Replace synthetic 1000-byte progress reporting in `GemmaOnDemandResourceBridge.swift` with genuine progress/fraction reporting. **Done 13 Sep 2026**: was rescaling `fractionCompleted` against a fabricated `total: Int64 = 1000` instead of reporting real bytes; now observes `completedUnitCount`/`totalUnitCount` directly off the same `NSProgress`, guarded against `totalUnitCount <= 0` (unset before the download size is known). No Kotlin-side change needed — `IosGemmaBaseModelInstaller.ios.kt` only ever consumes the derived fraction, which is unchanged by this fix. Verified via `:composeApp:linkDebugFrameworkIosSimulatorArm64` and an `xcodebuild` simulator build of the `iosApp` scheme (both green). **Real-device ODR download verification deferred to the next App Review submission** — user will check it then; not verified on-device yet.
- [x] Fix stale doc mentions of deleted parsers (`PayslipTextParser`, `DynamicSpatialParser`, `PayslipTokenParser`) in `CLAUDE.md` and `AI_INSIGHTS_PIPELINE.md`. **Done 13 Sep 2026**: confirmed via `git log` that all three were deleted (commits `365153b`/`8c175b9`/`0821fab`) but both docs still described them as present/dead-but-compiled; corrected `CLAUDE.md`'s architecture bullet and rewrote `AI_INSIGHTS_PIPELINE.md` §6 plus its stale SWOT/backlog/key-file-reference mentions to reflect the deletion (historical changelog entries left untouched — they're accurate for the point in time they describe).
- [x] Update `AI_INSIGHTS_PIPELINE.md` to describe actual ODR (On-Demand Resources) delivery mechanism instead of the old Background Assets note. **Done 13 Sep 2026**: doc still described the unbuilt/blocked Background Assets plan as current; `git log`/code (`GemmaOnDemandResourceBridge.swift`, commit `9fa3347`) showed it was already replaced by Apple On-Demand Resources and verified end-to-end on a physical device. Rewrote the executive summary, Stage 6 description, SWOT threat, and Things-To-Do items; added the missing changelog entry for the switch.
- [x] Rename `PayslipTokenParserGemmaTest.kt` to reflect that it tests `GrammarAwareParser` fallback. **Done 13 Sep 2026**: renamed file and class to `GrammarAwareParserGemmaTest` (test bodies unchanged); updated the stale name reference in `AI_INSIGHTS_PIPELINE.md` §6 and the pointer comment in `ReplicaUtilsTest.kt` (the §10 changelog entry mentioning the old name was left as-is — historical, accurate for the point in time it describes).
- [ ] Prune redundant/overly-broad R8 keep rules in `composeApp/proguard-rules.pro` (Koin annotations rule, unscoped native keep, broad LiteRT-LM package keep).
- ~~Consolidate the 9 scattered string files in `composeApp/src/commonMain/.../ui/theme/` into structured sub-catalogs.~~ **Struck off 13 Sep 2026**: checked — the split is already a deliberate, documented consequence of the 300-line rule (two files carry explicit comments to that effect), each object is already domain-scoped (Premium/Tax Planner/Legal/Insights/Import/Gemma/Support/SalaryRibbon), and Rule 4's actual SSOT concern (no hardcoded UI strings, all copy centralized) is already satisfied. Consolidating further would touch 130+ call sites across composeApp (`AppStrings` alone: 63 files) for no behavioral gain, and risks incorrectly merging coincidentally-identical strings (e.g. several unrelated "Edit"/"Cancel" labels) that may need to diverge later. Not worth the regression risk.

---

### Not on any sprint — already decided, leave alone
- Legacy/Modern strategy-set duplication — CLAUDE.md already documents this as intentional, not new debt
- Pre-scrub PII in git history — already a known, deliberately deferred decision
- Gemma model-fetch script requiring a local binary — developer-experience only, no user impact