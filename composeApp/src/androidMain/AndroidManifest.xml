<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:name=".PayslipApplication"
        android:allowBackup="false"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PayslipMax"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:theme="@style/Theme.PayslipMax.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="androidx.activity.ComponentActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Material.NoActionBar" />

        <!-- Not used: app has no Google Sign-In code path (Firebase Anonymous Auth only).
             This activity is pulled in transitively via firebase-auth-ktx -> credentials-play-services-auth
             and was misattributed as a real crash surface (Crashlytics issue add2187eac1b73536c614996b0cea686 —
             root-caused to Play pre-launch/Test Lab crawler infra, not real users). Removed from the merged
             manifest to eliminate the surface entirely. tools:ignore="MissingClass" suppresses a lint false
             positive: the class is genuinely resolved on the classpath (confirmed via dependencies task) but
             lint's MissingClass check can't see library-internal classes referenced only via node="remove". -->
        <activity
            android:name="com.google.android.gms.auth.api.signin.internal.SignInHubActivity"
            tools:node="remove"
            tools:ignore="MissingClass" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
    </application>

</manifest>
